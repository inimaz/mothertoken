---
interface Props {
  activeTab: 'home' | 'benchmark' | 'cli' | 'methodology';
}
const { activeTab } = Astro.props;

const tabs = [
  { id: 'home',        label: 'home',        href: '/'            },
  { id: 'benchmark',   label: 'benchmark',   href: '/benchmark'   },
  { id: 'cli',         label: 'cli',         href: '/cli'         },
  { id: 'methodology', label: 'methodology', href: '/methodology' },
];
---

<nav class="flex items-center justify-between mt-6 mb-16 animate-fade-in-up">
  <a href="/" class="flex items-center gap-2 text-[15px] font-semibold text-text tracking-tight no-underline group">
    <img src="/favicon.svg" alt="" class="h-5 w-5" />
    <span>mothertoken<span class="text-text-tertiary group-hover:text-text transition-colors duration-300">_</span></span>
  </a>
  <div class="flex items-center gap-1 bg-bg-secondary p-1 rounded-lg border border-border">
    {tabs.map(tab => (
      <a
        href={tab.href}
        class:list={[
          'font-mono text-[11px] px-[14px] py-[6px] rounded-md cursor-pointer transition-all duration-300 ease-quint no-underline select-none',
          activeTab === tab.id 
            ? 'bg-bg text-text shadow-sm border border-border-strong' 
            : 'text-text-tertiary hover:text-text hover:bg-border border border-transparent'
        ]}
      >
        {tab.label}
      </a>
    ))}
  </div>
</nav>
